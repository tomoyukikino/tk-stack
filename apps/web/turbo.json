{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "env": ["PUBLIC_API_URL", "PUBLIC_WEB_URL", "PROD"],
      "inputs": ["$TURBO_DEFAULT$", ".env"],
      "persistent": true
    },
    "build": {
      "env": ["PUBLIC_API_URL", "PROD"],
      "inputs": ["$TURBO_DEFAULT$", ".env"]
    }
  }
}
