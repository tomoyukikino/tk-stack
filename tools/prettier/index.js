/** @typedef {import("prettier").Config} PrettierConfig */

/** @type { PrettierConfig } */
const config = {
  arrowParens: 'avoid',
  printWidth: 120,
  singleQuote: true,
  semi: false,
  plugins: [],
}

export default config
